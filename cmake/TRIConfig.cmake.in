@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@TRI_USE_BLAS@)
    find_dependency(BLAS)
    find_dependency(LAPACK)
endif()

if(@TRI_USE_OPENMP@)
    find_dependency(OpenMP)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/TRITargets.cmake")

check_required_components(TRI)